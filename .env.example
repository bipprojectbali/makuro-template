# App
NODE_ENV=development
# Note: pick a free port. On this machine :3000 is taken by another container.
PORT=3005
APP_URL=http://localhost:3005

# Database — point at your Postgres. Example uses the shared infra-postgres.
DATABASE_URL=postgres://bip:makuro@localhost:5432/makuro

# Better Auth
# generate a secret: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-run-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3005
