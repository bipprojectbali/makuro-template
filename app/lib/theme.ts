import { createTheme } from '@mantine/core';

/**
 * Mantine default theme. Extend here (colors, fonts, radius, component
 * defaults) if needed; empty means Mantine's out-of-the-box defaults apply
 * app-wide via MantineProvider in app/root.tsx.
 */
export const theme = createTheme({});
